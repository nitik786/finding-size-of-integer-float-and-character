#include<stdio.h>
int main(){
	int a;
	char c;
	float f;
	printf("the size of int is %d\n",sizeof(int));
	printf("the size of char is %d\n",sizeof(char));
	printf("the size of float is %d\n",sizeof(float));
	return 0;
}
